name: expenses

services:
  pg:
    image: bitnami/postgresql:15.10.0
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=expenses